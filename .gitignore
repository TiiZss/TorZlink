node_modules/
dist/
.DS_Store
downloads/
# Local secrets — never commit (keep tracked: .env.example, **/.env*.example)
.env
.env.local
.env.*.local
.env.bak
.agents/
.claude/
.cursor/
.github/agents/
debug-*.log
*.bak
sbom.cdx.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
