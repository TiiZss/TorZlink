# Telegram (optional) — bot must be admin of the channel to post
# Uncomment and set your real values; do not use the placeholder token below.
# TELEGRAM_ENABLED=1
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHANNEL_ID=@your_channel
# or numeric channel id: -1001234567890

# Optional: require Bearer token on /api/* (recommended on shared Docker networks)
# TORZLINK_SERVE_TOKEN=change-me-long-random

# NAS deploy-from-dev (local only — never commit real passwords)
# NAS_HOST=192.168.1.5
# NAS_USER=puper
# NAS_PASSWORD=
# PROXY_NET_NAME=0-nas_proxy_net
# TORZLINK_DEPLOY_DIR=/volume2/Docker_Configs/torzlink-deploy
# TORZLINK_NETWORK_MODE=direct

# TorZlink paths (optional; legacy TORLINK_* still supported)
# TORZLINK_STATE_DIR=/data
# TORZLINK_DOWNLOAD_DIR=/downloads
# TORZLINK_DISABLE_NAT=1
# TORZLINK_SKIP_UPDATE=1
